<!DOCTYPE html>
<html lang="ko" xmlns:th="http://thymeleaf.org">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <h2>[ Thymeleaf 결과 출력 ]</h2>
    <div><a th:href="@{/}"><img th:src="@{/images/home.png}" alt="">홈으로</a></div>
    <p>
        <ul>
            <li>한글:<span th:text="${kor}">한글</span></li>
            <li>영문:<span th:text="${eng}">영문</span></li>
            <li>일어:<span th:text="${jap}">일어</span></li>
            <li>태그:<span th:text="${tag}">태그</span></li>
            <li>태그인식:<span th:utext="${tag}">태그</span></li><br>
           
			<li><span th:text="${age}">정수</span></li>
			<li><span th:text="${pi}">실수</span></li>
            <li>
            	<a th:href="${url}">네이버로 이동</a>		<!-- @는 내 서버에서만 --> <!-- $는 변수 안에 있는걸 꺼내서 사용 -->
            </li><br>
            
            <li><span th:text="${nullData}">널값</span></li>
            <li><span th:text="${emptyData}">빈문자열</span></li>
        </ul>
    </p>
</body>
</html>